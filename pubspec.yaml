name: portfolio_steve
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.11.0

dependencies:
  flutter:
    sdk: flutter
  flutter_web_plugins:
    sdk: flutter
  get_it: ^9.2.0
  go_router: ^17.1.0
  google_fonts: ^8.0.1
  provider: ^6.1.5+1
  url_launcher: ^6.3.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  mocktail: ^1.0.4

flutter:
  uses-material-design: true

assets:
  - assets/data/profile.json
  - assets/data/projects.json
  - assets/data/experiences.json
  - assets/data/skills.json
  - assets/images
